# Print method for anon_context_collection

Displays combined anonymized objects in a format suitable for LLM
prompts. Each object is clearly delineated and presented with
appropriate context.

## Usage

``` r
# S3 method for class 'anon_context_collection'
print(x, ...)
```

## Arguments

- x:

  An anon_context_collection object

- ...:

  Additional arguments (currently unused)

## Value

Invisibly returns x
